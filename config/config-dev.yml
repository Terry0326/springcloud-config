server:
  port: 8001
spring:
  application:
    name: cloud-payment-service
  datasource:
    #type: com.zaxxer.hikari.util.DriverDataSource
    driver-class-name: org.gjt.mm.mysql.Driver
    url: jdbc:mysql://103.44.145.182:13306/bic-log?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false&allowMultiQueries=true&useSSL=false
    username: root
    password: qwer`123test
    type: com.alibaba.druid.pool.DruidDataSource

eureka:
  client:
    # 表示是否将自己注册进EurekaServer默认为true
    register-with-eureka: true
    # 是否从EurekaServer抓取已有得注册信息，默认为true.
    # 单节点无所谓，集群必须设置为true才能配合ribbon使用负载均衡
    fetch-registry: true
    service-url:
      #defaultZone: http://localhost:7001/eureka
      defaultZone: http://eureka7002.com:7002/eureka,http://eureka7001.com:7001/eureka

  instance:
    instance-id: payment
    prefer-ip-address: true
    #Eureka客户端向服务端发送心跳的时间间隔 单位为秒（默认是30秒）
    lease-renewal-interval-in-seconds: 1
    #Eureka服务端在收到最后一次心跳后等待时间上限，单位为秒（默认是90秒），超时将剔除服务
    lease-expiration-duration-in-seconds: 2

mybatis:
  type-aliases-package: com.terry.springcloud.entities
  mapper-locations: classpath*:**/*Mapper.xml
  #config-location: classpath:mybatis-config.xml
